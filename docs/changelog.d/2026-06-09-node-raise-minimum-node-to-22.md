**Changed** / **BREAKING** — Raise the minimum supported Node.js to 22
(`engines.node` `>=20.20.0` → `>=22`); Node 20 reached end-of-life on
2026-04-30, so the floor moves to the lowest LTS still receiving updates.
See the [migration fragment](../migrations.d/2026-06-09-node-raise-minimum-node-to-22.md).
