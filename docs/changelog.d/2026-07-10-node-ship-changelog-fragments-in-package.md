**Added** — The published npm package now ships the repository's
`changelog.d/` and `migrations.d/` fragment folders (staged into the tarball
at build time), so the installed copy under `node_modules/` carries a
version-exact changelog.
